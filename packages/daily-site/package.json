{
  "name": "daily-site",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "yarn tsc --watch --outDir dist",
    "build": "yarn tsc --outDir ../../.scripts",
    "test": "echo test"
  },
  "devDependencies": {
    "@types/node": "^16.0.0",
    "typescript": "^4.1.3"
  }
}
